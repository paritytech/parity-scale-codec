[package]
name = "parity-codec-derive"
description = "Serialization and deserialization derive macro for Parity Codec"
version = "1.0.0"
authors = ["Parity Technologies <admin@parity.io>"]
license = "Apache-2.0"

[lib]
proc-macro = true

[dependencies]
syn = "0.14"
quote = "0.6"
proc-macro2 = "0.4"

[dev-dependencies]
parity-codec = { path = "../", version = "~1.0" }

[features]
default = ["std"]
std = []
